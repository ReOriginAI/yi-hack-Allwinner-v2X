#!/bin/bash
set -e

SCRIPT_DIR=$(cd `dirname $0` && pwd)
cd "$SCRIPT_DIR"

[ -x ./_install/bin/motiond ] || { echo "motiond binary missing" >&2; exit 1; }
mkdir -p ../../build/yi-hack/bin
cp ./_install/bin/motiond ../../build/yi-hack/bin/motiond
chmod 0755 ../../build/yi-hack/bin/motiond
