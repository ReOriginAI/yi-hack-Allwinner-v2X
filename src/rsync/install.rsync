#!/bin/bash
set -e

SCRIPT_DIR=$(cd `dirname $0` && pwd)
cd "$SCRIPT_DIR"

mkdir -p ../../build/yi-hack/bin
cp -f ./_install/bin/rsync ../../build/yi-hack/bin/rsync
chmod 0755 ../../build/yi-hack/bin/rsync
